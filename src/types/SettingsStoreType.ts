export type SettingsContextType = {
  darkMode: boolean;
  setDarkMode: (data: boolean) => void;
  themeBackground: boolean;
  setThemeBackground: (data: boolean) => void;
  imperialUnits: boolean;
  setImperialUnits: (data: boolean) => void;
};
